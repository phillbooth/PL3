{
  "name": "@lo/cli",
  "version": "0.1.0",
  "description": "LO developer command-line interface for checking, building, serving, reporting and running safe tasks.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "bin": {
    "lo": "./dist/index.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "npm run typecheck"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
