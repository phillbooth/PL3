secure flow responsePayload() -> Json {
  return {
    "items": [
      { "id": 1, "test": "xxx" },
      { "id": 2, "test": "xxx" },
      { "id": 3, "test": "xxx" }
    ]
  }
}

secure flow main() -> Result<Void, Error> {
  let payload: Json = responsePayload()
  console.log("json ids: 1,2,3 test: xxx")
  return Ok()
}
