{
  "name": "@lo/app-kernel",
  "version": "0.1.0",
  "description": "Optional LO secure runtime kernel design for typed APIs, validation, auth, workload controls, jobs and reports.",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "test": "npm run test:hello && npm run test:vector && npm run test:sum && npm run test:decimal && npm run test:json",
    "test:hello": "node ../lo-core/compiler/lo.js run tests/hello-world.lo",
    "test:vector": "node ../lo-core/compiler/lo.js run tests/vector-function.lo",
    "test:sum": "node ../lo-core/compiler/lo.js run tests/sum.lo",
    "test:decimal": "node ../lo-core/compiler/lo.js run tests/decimal-sum.lo",
    "test:json": "node ../lo-core/compiler/lo.js run tests/json-return.lo"
  }
}
